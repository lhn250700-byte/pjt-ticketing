global:
  scrape_interval: 1s # ⏳ 1초마다 데이터를 긁어옵니다. (테스트용 초고속 수집)

scrape_configs:
  - job_name: 'spring-boot-actuator'
    metrics_path: '/actuator/prometheus' # 🍏 아까 열어둔 주소 경로
    static_configs:
      - targets: ['host.docker.internal:8080'] # 🐳 도커 컨테이너 내부에서 내 PC(스프링 부트)로 접속하는 마법의 주소